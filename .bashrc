export PATH=$HOME/bin:$PATH
#export PS1='\W$ '
export PS1='$ '
# export GREP_COLOR='always'

alias rm='rm -i'
alias ll='ls -laF'
alias la='ls -A'
alias l='ls -CF'
alias today="date '+%Y%m%d'"

eval "$(rbenv init -)"
